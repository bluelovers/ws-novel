{
  "name": "ws-novel",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sort-package-json": "oao all sort-package-json2",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "resolutions": {},
  "devDependencies": {
    "@types/node": "^10.0.9"
  }
}
